'''
perfil de gift
repositorio
new
ponerlo en privado
no poner a README
por ahora todo a none
crear
abrir: programa git
poner $ cd desktop/(si pongo tab se autocompleta)
$ cd (nombre de carpeta)/
$git init
$ git branch -M main
$ git add (el archivo que tengo en la carpeta) 
$ git status
$ git commit -m "(comentario como 'agregando x cosa')"
$ git remote add origin (link de mi git con mi carpeta)
$ git push origin main
$ git reset (nombre del archivo) -> para borrar
$git status
$git add (todo lo mismo para subir xd)

puedo ir al perfil de alguien, a una carpeta
voy al link de la derecha de su carpeta y copio el link
voy al programa git -> $git clone (el link) -> creo :c

'''

'''from funciones(aqui va el nombre sino) import * -> para hacer una biblioteca'''